
# 文件名分析与代码清理计划（修订版）

## 一、文件名分析

### 需要修正的文件名
1. **`DTVars.java`** → `TQLSVars.java` - 与模组名保持一致（可选但推荐）
2. **`TQUtls.java`** → `TQUtils.java` - 修正拼写错误（该文件被使用，不能删除）

## 二、确认"有功能但没有使用"的文件

经过分析，只有一个文件符合条件：
- **`ExtendedParser.java`** - 定义了完整的解析功能，但没有被任何其他代码调用或引用

## 三、其他文件状态分析

### 被使用的文件（保留）
| 文件 | 被谁使用 | 功能 |
|------|----------|------|
| PWIntegration.java | TQLSlib.loadContent() | ClassMap注册 |
| TranqolIntegration.java | TQLSlib.loadContent()/init() | 初始化集成模块 |
| TQTechTree.java | TranqolIntegration, PWIntegration | 科技树框架 |
| MxStats.java | PWIntegration, PowerTower | 自定义Stat定义 |
| ExtentionStatus.java | PWIntegration, ExtendedParser | 状态效果 |
| TQDialogs.java | TranqolIntegration, PWIntegration | 对话框框架 |
| TQDrawf.java | TranqolIntegration, PWIntegration | 绘图工具 |
| TQShaders.java | TranqolIntegration, PWIntegration | Shader框架 |
| TQUtls.java | TranqolIntegration, PWIntegration | 工具类 |
| PowerTower.java | PWIntegration, ExtendedParser | 方块定义 |
| 其他方块类 | PWIntegration, ExtendedParser | 方块定义 |

## 四、执行步骤

1. **删除** `src/tqlslib/ExtendedParser.java` - 唯一有功能但未使用的文件
2. **修改** `PWIntegration.java` - 移除对 ExtendedParser 的导入（已不再需要）
3. **（可选）重命名** `DTVars.java` → `TQLSVars.java`
4. **（可选）重命名** `TQUtls.java` → `TQUtils.java`（同步修改引用）
5. 编译验证
